global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'digiwallet-backend'
    static_configs:
      # On Windows/macOS Docker Desktop, host.docker.internal resolves to the host machine
      - targets: ['host.docker.internal:8080']
    metrics_path: /metrics
